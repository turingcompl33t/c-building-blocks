// accumulate.c
// Accumulate algotithm on integer arrays.

#include "accumulate.h"

int accumulate(
    int array[], 
    size_t first, 
    size_t last, 
    int start, 
    binary_op_f op)
{
    return 0;
}